<h1 align = "center"><%= @rec_expense.name %></h1>
    <div class="col-lg-6 col-lg-offset-3">
        <div align = "center" class ="well">

            <h4><strong> Duration of the reminder:</strong> <%= @rec_expense.duration %><br/><br/>
            <h4><strong> Start date:</strong> <%= @rec_expense.reminder_date.to_s(:long) %><br/><br/>
            
            <div class="expense-actions">
                <br/>
                <%= link_to "Edit", edit_recurring_expense_path(@rec_expense), class: "btn btn-xs btn-primary" %>
                <%= link_to "Delete", recurring_expense_path(@rec_expense), method: :delete, data: { confirm: "Are you sure you want to delete the expense?"}, class: "btn btn-xs btn-danger" %>
                <%= link_to "Back", recurring_expenses_path, class: "btn btn-xs btn-success" %>
            </div>
        </div>
    </div>
