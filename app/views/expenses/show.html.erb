<h1 align = "center"><%= @expense.name %></h1>
    <div class="col-lg-6 col-lg-offset-3">
        <div align = "center" class ="well">

            <h4><strong> Amount spent:</strong> <%= @expense.amount %><br/><br/>
            <h4><strong> Date:</strong> <%= @expense.date.to_s(:long) %><br/><br/>
            <h4><strong> Category:</strong> <%= @expense.category.name if @expense.category %><br/><br/>
            <div class="expense-actions">
                <br/>
                <%= link_to "Edit", edit_expense_path(@expense), class: "btn btn-xs btn-primary" %>
                <%= link_to "Delete", expense_path(@expense), method: :delete, data: { confirm: "Are you sure you want to delete the expense?"}, class: "btn btn-xs btn-danger" %>
                <%= link_to "Back", expenses_path, class: "btn btn-xs btn-success" %>
            </div>
        </div>
    </div>
